/** Marketing site (Project 1 on Vercel — `site/` folder). */
export const MARKETING_URL =
  process.env.EXPO_PUBLIC_MARKETING_URL ?? 'https://makefacelessvideos.com';

/** Web app (Project 2 — `npm run build:web` → `dist/`). */
export const APP_URL =
  process.env.EXPO_PUBLIC_APP_URL ?? 'https://app.makefacelessvideos.com';
